# Repository upload note

This repository intentionally excludes local Android signing keys (`*.jks`), IDE workspace metadata, and machine-local configuration. These files are not required to open or build the source project and must not be committed to a public repository.

The project source and runtime assets are maintained from the hardened KM Takip source package.